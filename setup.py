from setuptools import setup

setup(
    name='jsonobject',
    version='0.0.1',
    author='Danny Roberts',
    packages=['jsonobject'],
    install_requires=[
    ],
    tests_require=['unittest2'],
    test_suite='tests',
)
